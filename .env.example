# Copy this file to .env and fill in values as needed.
# Never commit your real .env to git.

# local | motherduck
DB_MODE=local

# Path for local DuckDB file (used when DB_MODE=local)
LOCAL_DUCKDB_PATH=./data/app.duckdb

# MotherDuck: set this to your token (kept secret). Do not quote.
# Example of secure export in shell (preferred over editing .env directly):
#   export MOTHERDUCK_TOKEN=$(security find-generic-password -s motherduck_token -w)
MOTHERDUCK_TOKEN=REPLACE_WITH_{{MOTHERDUCK_TOKEN}}

# Name of the MotherDuck database to use/create (when DB_MODE=motherduck)
# If omitted, connection will use your MotherDuck default context.
MOTHERDUCK_DATABASE=api_stress_test_dev
